import React from 'react';
import { DialogsWrapper } from './DialogStyled';

const Dialogs = () => {
    return (
        <>
            <DialogsWrapper className='dialogs-wrapper'>Dialogs with test reset</DialogsWrapper>
        </>
    );
};

export default Dialogs;
